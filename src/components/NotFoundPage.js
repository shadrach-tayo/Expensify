import React from 'react';
import { NavLink } from 'react-router-dom';

const NotFoundPage = () => (
  <div>
    this is the NotFoundPage <NavLink to="/">Go home</NavLink>
  </div>
);

export default NotFoundPage;
